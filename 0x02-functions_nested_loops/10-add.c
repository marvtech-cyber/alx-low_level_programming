#include "main.h"

/**
 * add - adds two integers and returns result
 * @a: first parameter
 * @b: second parameter
 *
 * Return: result of addition
int add(int a, int b)
{
    int c = a + b;
    return (c);
}
