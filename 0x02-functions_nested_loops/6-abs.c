#include "main.h"
#include <stdlib.h>

/**
 * _abs - entry point
 * @n: parameter integer to find absolute value
 *
 * Description: checks the absolute value of an integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
		return (n);
}
